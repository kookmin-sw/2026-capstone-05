# GridInventory 시스템 테스트 가이드

## ⚡ 빠른 테스트 시작

### 🎯 1단계: GameObject 생성
1. Unity 씬에서 **빈 GameObject 생성**
2. 이름을 **"GridInventoryUI_Test"**로 변경
3. 다음 컴포넌트들을 **순서대로** 추가:
   ```
   • GridInventorySystem
   • GridInventoryUI  
   • UIDocument
   • GridInventoryTester
   ```

### 🎯 2단계: UIDocument 설정
1. **UIDocument** 컴포넌트에서:
   - **Panel Settings**: `Assets/_Project/Scripts/01_PM/Inventory/UI/PanelSettings.asset`
   - **Source Asset**: `Assets/_Project/Scripts/01_PM/Inventory/UI/Inventory.uxml`

### 🎯 3단계: GridInventoryUI 설정
1. **GridInventoryUI** 컴포넌트에서:
   - **Ui Document**: 위에서 생성한 UIDocument 컴포넌트 드래그 앤 드롭
   - **Style Sheet**: `Assets/_Project/Scripts/01_PM/Inventory/UI/Inventory.uss`
   - **Panel Name**: `"NEW INVENTORY"` (기존 시스템과 구분용)

### 🎯 4단계: GridInventorySystem 설정
1. **GridInventorySystem** 컴포넌트에서:
   - **Inventory UI**: 위의 GridInventoryUI 컴포넌트 드래그 앤 드롭
   - **Grid Width**: `8` (기본값)  
   - **Grid Height**: `10` (기본값)

### 🎯 5단계: 테스트 아이템 준비
1. **Player의 ItemData** ScriptableObject 몇 개를 찾거나 생성
   - 필수 필드: `itemName`, `itemIcon`, `gridShape`
   - 위치: `Assets/_Project/Scripts/02_Player/Item/ItemData/` 폴더
2. **GridInventoryTester** 컴포넌트에서:
   - **Use New Grid System**: ✅ **체크**
   - **Test Items**: ItemData들을 배열에 드래그 앤 드롭
   - **Test Quantities**: 각 아이템별 수량 설정 (예: 1, 5, 3)

---

## 🎮 테스트 조작법

### 키보드 조작
| 키 | 기능 | 설명 |
|---|---|---|
| **Tab** | 인벤토리 열기/닫기 | NEW INVENTORY 창 토글 |
| **T** | 아이템 추가 | 설정한 테스트 아이템들 인벤토리에 추가 |
| **C** | 인벤토리 비우기 | 모든 아이템 제거 |
| **드래그** | 아이템 이동 | 마우스로 아이템을 끌어서 이동 |

### 마우스 조작
- **좌클릭 + 드래그**: 아이템 이동
- **우클릭**: 아이템 장착 (PlayerEquipment 연동, 추후 구현)

---

## ✅ 테스트 체크리스트

### 🔧 기본 기능 테스트
- [ ] **Tab키**를 눌러서 "NEW INVENTORY" 창이 나타나는가?
- [ ] **T키**를 눌러서 콘솔에 아이템 추가 로그가 나오는가?
- [ ] **인벤토리에 아이템 아이콘**이 정상적으로 표시되는가?
- [ ] **아이템을 드래그**해서 다른 위치로 이동할 수 있는가?
- [ ] **C키**로 인벤토리를 비울 수 있는가?

### 🎯 고급 기능 테스트
- [ ] **큰 크기 아이템** (2x1, 1x2 등)이 올바른 크기로 표시되는가?
- [ ] **인벤토리가 가득 찬 상태**에서 T키를 누르면 "공간 부족" 메시지가 나오는가?
- [ ] **인벤토리가 열렸을 때** 마우스 커서가 나타나고 플레이어 조작이 차단되는가?
- [ ] **인벤토리가 닫혔을 때** 마우스 커서가 숨겨지고 플레이어 조작이 복구되는가?

### 🔍 UI/UX 테스트
- [ ] **탭 버튼**들이 기존 UXML 레이아웃대로 표시되는가?
- [ ] **좌우 분할**된 레이아웃이 정상적으로 보이는가?
- [ ] **아이템 수량**이 1개 이상일 때 숫자가 표시되는가?
- [ ] **스타일시트**가 올바르게 적용되어 시각적으로 깔끔한가?

---

## 🐛 문제 해결

### "GridInventorySystem을 찾을 수 없습니다" 에러
**원인**: GridInventoryTester가 GridInventorySystem을 찾지 못함  
**해결**: 같은 GameObject에 GridInventorySystem 컴포넌트가 있는지 확인

### "UXML에서 'container' 클래스를 찾을 수 없습니다" 에러  
**원인**: UIDocument에 올바른 UXML 파일이 할당되지 않음  
**해결**: Source Asset에 `Inventory.uxml` 파일이 올바르게 할당되어 있는지 확인

### Tab키를 눌러도 인벤토리가 안 나타남
**원인**: UIDocument나 StyleSheet 설정 문제  
**해결**: 
1. Panel Settings가 할당되어 있는지 확인
2. Style Sheet가 올바르게 연결되어 있는지 확인  
3. 콘솔에서 에러 메시지 확인

### T키를 눌러도 아이템이 안 추가됨
**원인**: ItemData 설정 또는 gridShape 누락  
**해결**:
1. Test Items 배열에 ItemData가 할당되어 있는지 확인
2. ItemData에 `gridShape` 필드가 설정되어 있는지 확인
3. `itemName`과 `itemIcon`도 설정되어 있는지 확인

### 아이템 아이콘이 안 보임
**원인**: ItemData의 itemIcon이 null이거나 스타일 문제  
**해결**:
1. ItemData의 itemIcon에 Sprite가 할당되어 있는지 확인  
2. Inventory.uss 파일이 올바르게 연결되어 있는지 확인

---

## 🚀 성공적인 테스트 시나리오

### 📋 완벽한 테스트 순서
1. **GameObject 생성 및 컴포넌트 추가** (1-4단계)
2. **플레이 모드 진입**
3. **Tab키** → "NEW INVENTORY" 창 확인  
4. **T키** → 콘솔에서 아이템 추가 성공 메시지 확인
5. **인벤토리에서 아이템 시각적 확인** (아이콘, 위치)
6. **마우스로 아이템 드래그** → 위치 이동 확인
7. **T키 여러 번** → 인벤토리가 가득 찰 때까지 추가
8. **공간 부족 메시지** 확인
9. **C키** → 인벤토리 비우기 확인
10. **Tab키** → 인벤토리 닫기, 마우스/플레이어 입력 복구 확인

### 🎉 테스트 성공 시 기대 결과
- ItemData 기반 아이템들이 그리드에 정확하게 배치됨
- Player 개발자의 아키텍처와 완벽하게 연동됨  
- 기존 UXML 레이아웃의 세련된 UI 활용
- 드래그 앤 드롭, 입력 차단 등 모든 기능 정상 작동

**이제 테스트 준비 완료! 🎮**

## 1. 아이템 데이터 생성 및 설정 (ItemDetails)
1. Project 창 빈 공간 우클릭 -> `Create > Inventory > Item` 클릭하여 아이템 데이터(ScriptableObject) 생성
2. 인스펙터 창에서 다음 정보들을 설정:
   - **Name**: 아이템 이름 (예: 사과, 검)
   - **Max Stack**: 한 슬롯에 겹칠 수 있는 최대 개수 (기본값: 1)
   - **Width / Height**: 인벤토리 그리드에서 아이템이 차지하는 칸 수 (예: 1x1, 가로 1x세로 2 등)
   - **Id**: 고유 식별자 (우측 점 3개 메뉴 `ContextMenu` - "Assign New Guid"를 통해 재할당 가능)
   - **Icon**: 인벤토리 UI에 표시될 아이템 이미지 (Sprite)
   - **Description**: 아이템 설명

## 2. UI 설정 (UI Toolkit 기반)
이 시스템은 uGUI(Canvas)가 아닌 **UI Toolkit**으로 제작되었습니다.
1. 씬에 빈 GameObject를 생성 (예: `InventoryUI`)
2. `UIDocument` 컴포넌트 추가
   - **Panel Settings**: 프로젝트에 있는 UI Panel Settings 에셋 할당
   - **Source Asset**: `Inventory.uxml` 에셋 할당
3. 동일한 GameObject에 `InventoryView.cs` 스크립트 추가
4. 인스펙터 설정:
   - **Document**: 방금 추가한 UIDocument 연결
   - **Style Sheet**: `Inventory.uss` 에셋 연결
   - **Panel Name**: 창 상단에 표시될 헤더 이름 (기본값: "Inventory")

## 3. 인벤토리 메인 연결 (Inventory)
1. 플레이어나 별도의 관리 오브젝트에 `Inventory.cs` 컴포넌트를 추가
2. 인스펙터 설정:
   - **View**: 위 2번에서 설정한 `InventoryView` 컴포넌트를 드래그 앤 드롭
   - **Starting Items**: 게임 시작 시 들고 시작할 초기 아이템 목록(`ItemDetails`)을 리스트에 추가

## 4. 조작 및 연동 (입력)
인벤토리 On/Off는 `InventoryView.cs`에서 `Tab` 키로 하드코딩되어 있습니다. 별도 외부 입력 설정 없이 바로 동작합니다.
- **Tab 키 누름**: 인벤토리 창 표시/숨김 토글
- **열렸을 때 (Opened)**:
  - 마우스 커서 활성화 및 잠금 해제 (`Cursor.visible = true`, `CursorLockMode.None`)
  - 씬 내의 `PlayerInputHandler`를 자동으로 찾아 `SetInputActive(false)`를 호출해 플레이어 조작 차단
- **닫혔을 때 (Closed)**:
  - 마우스 커서 숨김 및 잠금 (`Cursor.visible = false`, `CursorLockMode.Locked`)
  - `PlayerInputHandler` 조작 다시 활성화

## 5. 핵심 작동 방식 비교
### 5.1 그리드 크기 차지 (Grid-based)
- 디아블로 혹은 타르코프처럼 아이템의 `Width`와 `Height` 옵션에 따라 그리드의 여러 칸을 차지합니다.

### 5.2 드래그 앤 드롭 기능
- 아이템 아이콘을 좌클릭하여 드래그하고, 원하는 그리드 위치에 놓을 수 있습니다.
- 배치 가능한 빈 공간이 있으면 그대로 들어갑니다.

### 5.3 아이템 스태킹 (Stacking) / 합치기
- 동일한 Id의 아이템이고 `maxStack` 값이 1 이상이라면 겹쳐서 수량을 합칠 수 있습니다.

### 5.4 스왑 (Swapping)
- 이미 아이템이 있는 자리에 다른 아이템을 덮어씌우면, 칸 계산 판정 후 두 아이템의 위치가 서로 뒤바뀝니다(Swap).

## 6. 중요 참고 & 디버깅 포인트
- **UI 사이즈 자동 계산**: 인벤토리 칸의 가로 개수는 기본 8칸으로 세팅되며, 총 Capacity 개수 대비 열 수에 맞춰 세팅됩니다.
- **아이템 최대 크기 주의**: 인벤토리 폭(8칸)을 초과하는 Width를 가진 아이템은 넣거나 배치할 때 에러가 발생하거나 드롭되지 않을 수 있습니다.
- **`PlayerInputHandler` 선택사항**: 씬에 `PlayerInputHandler`가 없어도 인벤토리는 정상 동작합니다. 단, 존재할 경우 UI를 열고 닫을 때 자동으로 플레이어의 움직임을 멈추거나 재개합니다.
- UI Toolkit 에러 발생 시 UI Builder 에디터로 `Inventory.uxml`을 열어 `container`나 `inventory-window` 등 지정된 클래스/이름 구조가 손상되지 않았나 점검하세요.
